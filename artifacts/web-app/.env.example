# Web app environment
# Set VITE_API_URL to your deployed API base URL (no trailing slash).
# Examples:
#   Local dev (proxied through vite):  leave empty, vite proxies /api -> http://localhost:5000
#   Production (Netlify):              https://rewaya-hygiene-api.onrender.com
VITE_API_URL=
